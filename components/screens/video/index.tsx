import React, { FC } from "react";
import { View } from "react-native";
import { RootScreenProps } from "utils";

const VideoScreen: FC<RootScreenProps<'Video'>>  = () => {
    return <View>
        
    </View>
}